import React from 'react';

export default function () {
  return (
    <div
      style={{
        color: '#666',
        fontSize: '2rem',
        display: 'flex',
        width: '100%',
        height: '100%',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      Please Select an Image
    </div>
  );
}
